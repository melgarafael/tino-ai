{
  "name": "tino",
  "version": "0.1.0",
  "type": "module",
  "description": "Curador local-first de novidades de IA que roda no Claude Code + Obsidian",
  "private": true,
  "scripts": {
    "test": "node --test tests/",
    "serve": "python3 -m http.server 5173"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "yaml": "^2.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
